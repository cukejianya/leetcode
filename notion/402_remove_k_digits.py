class Solution:
    def removeKdigits(self, num: str, k: int) -> str:
        diff = len(num) - k
        start = 0
        end = diff
        min_num = min(num[start:diff])
        while(start + diff <= end)
            f
        
